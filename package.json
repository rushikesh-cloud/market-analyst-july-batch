{
  "name": "market-analyst-july-batch",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names frontend,backend --prefix-colors cyan,green \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "uv --directory backend run --env-file ../.env uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "worker": "uv --directory backend run --env-file ../.env python -m app.worker",
    "migrate": "uv --directory backend run --env-file ../.env python -m app.migrate",
    "install:all": "npm install && npm --prefix frontend install && uv --directory backend sync",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
